<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Recuperación</title>
    <script>
        let status = '<%- JSON.stringify(status) %>';
        if (JSON.parse(status) == 'Verificado') {
            if (confirm('Se actualizó la contraseña correctamente ¿Deseas ir a taskMaster?')) {
                window.location.href =  "http://localhost:5173/"
            }
        }
    </script>
</head>
<body>
    <div class="pass-container">
        <h1 class="title"><%= email%></h1>
        <form class="container-form" action="" method="post">
            <input class="input" type="password" name="password" placeholder="Contraseña nueva" id="password">
            <br>
            <input class="btn" type="submit" value="submit">
        </form>
    </div>

    <style>

        *{
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body{
            background-color: #034078;
            font-family: 'Roboto Mono', monospace;
        }

        .pass-container {
            width: 600px;
            margin: 7em auto;
            background-color: #E8E9F3;
            display: flex;
            justify-content: center;
            align-items: center;
            flex-direction: column;
            gap: 2em;
            padding: 2em 0;
        }

        .title {
            color: #272635;
            font-size: 2rem;
        }

        .container-form {
            width: 100%;
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
        }

        .input {
            width: 70%;
            font-size: 1.2rem;
            border-radius: 6px;
            border: black 1px solid;
            padding: .5em .5em;
        }

        .btn {
            padding: .5em 1em;
            border: none;
            background-color: #1282A2;
            font-weight: 700;
            font-size: 1rem;
        }

        .btn:hover{
            cursor: pointer;
            opacity: 0.8;
        }
    </style>
</body>
</html>